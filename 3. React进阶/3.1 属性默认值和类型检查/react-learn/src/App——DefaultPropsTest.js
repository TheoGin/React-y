import React from "react";
// import FuncDefaultProps from "./FuncDefaultProps";
import ClassDefaultProps from "./ClassDefaultProps";

function App(props) {
  return (
    // <FuncDefaultProps a={10} b={20} />
    <ClassDefaultProps a={10} b={20} />
  );
}

export default App;